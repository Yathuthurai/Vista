class Comments {
  constructor(id, owner, moment, comment, ownerId) {
    this.id = id;
    this.owner = owner;
    this.moment = moment;
    this.comment = comment;
    this.ownerId = ownerId;
  }
}

export default Comments;
